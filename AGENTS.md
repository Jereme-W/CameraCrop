## Nuke script workflow

- This repository is being used to iterate on a python script that will be copy-pasted directly into Nuke for testing before any PR is created.
- After modifying the main `.py` script, always include the COMPLETE updated contents of that file as a download.
- Put the full updated script downloadable file first, then any explanation after it.
